{
	"name": "espace-membre",
	"description": "Espace membre AFFBC : front statique consommant les API de gestion/boutique/calendrier via un jeton signé partagé.",
	"private": true,
	"devDependencies": {
		"typescript": "5.9.3",
		"vitest": "^2.1.0",
		"wrangler": "^4.110.0"
	},
	"scripts": {
		"cf-typegen": "wrangler types",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"check": "tsc --noEmit && node --check public/assets/app.js && vitest run && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	}
}
